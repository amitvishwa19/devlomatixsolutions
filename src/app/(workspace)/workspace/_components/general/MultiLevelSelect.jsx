import React from 'react'

export default function MultiLevelSelect({ categories, value, onValueChange, placeholder = "Select a category", className, }) {
    return (
        <div>MultiLevelSelect</div>
    )
}
