"use client";

import { ReactFlowProvider } from "@xyflow/react";
import "@xyflow/react/dist/style.css";

// This is a placeholder for the full Canvas component
export const Canvas = () => {
    return (
        <div className="flex h-full items-center justify-center bg-muted/20 font-mono text-xs text-muted-foreground">
            // canvas_placeholder (select a workflow to view)
        </div>
    );
};

export default Canvas;
